# AGENT RULES 🤖 (Procedural Memory - Step 76)

## 🛡️ SEGURIDAD (Aegis & DLP)
- **REGLA 1 (Privacidad)**: NUNCA escribas secretos, API Keys o PII en el código. Usa `process.env`.
- **REGLA 2 (Circuit Breaker)**: Si los tokens de "thoughts" superan el límite (3000), aborta inmediatamente.
- **REGLA 3 (Aegis)**: Prohibidos los comandos destructivos (`rm -rf`, `DROP TABLE`, etc.).

## 🏗️ ESTRUCTURA (FSD Lite)
- **REGLA 4 (Localidad)**: Toda la lógica de negocio en `features/`. No ensucies la raíz.
- **REGLA 5 (No Barrel Files)**: Prohibidos los `index.ts` de re-exportación. Importaciones directas.
- **REGLA 6 (Schema-First)**: Define siempre el esquema Zod `schemas/` antes de la UI o API.

## 💻 CÓDIGO & ESTILO
- **REGLA 7 (React)**: Usa componentes funcionales y Hooks. Prohibido Class Components.
- **REGLA 8 (Atomicidad)**: Si editas un archivo, genera el archivo completo. No envíes diffs parciales que puedan corromper el estado.
- **REGLA 9 (Consistencia)**: Sigue estrictamente Tailwind CSS y Shadcn/UI según `AGENTS.md`.

## 🧠 OPERACIONES
- **REGLA 10 (Protocolo)**: Lee `AGENTS.md` (Constitución) antes de iniciar. No contradigas el @TechStack.
- **REGLA 11 (Audit)**: Genera evidencia visual (`scripts/qa/capture_artifact.ts`) después de cualquier cambio en la UI.

ESTAS REGLAS SON EL FIREWALL COGNITIVO. NO SE NEGOCIAN.
